define(function (require) {

    // URLS used in the app
    return {
        viewSets: "mockViewSetUrl",
        mockUserData: "mockUserDataUrl",
        userSettings: "mockUserSettingsUrl"
    };
});